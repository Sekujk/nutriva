# dependencies
node_modules/

# Expo
.expo/
dist/
web-build/
expo-env.d.ts

# Native
.kotlin/
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Metro
.metro-health-check*

# debug
npm-debug.*
yarn-debug.*
yarn-error.*

# macOS
.DS_Store
*.pem

# Supabase CLI
supabase/.temp/

# local env files
.env
.env*.local

# typescript
*.tsbuildinfo

# generated native folders
/ios
/android

# herramientas de agentes/IA (config local, no forma parte de la app)
.claude/
CLAUDE.md
AGENTS.md

# notas y capturas internas, no se publican en el repositorio
docs/

# plantilla local de variables de entorno, no se publica en el repo
.env.example

# función servidor: se despliega manualmente desde el dashboard de Supabase,
# no tiene sentido publicar su código en un repo público
supabase/functions/delete-account/

# migraciones de base de datos: revelan el esquema completo de la app,
# se aplican manualmente con supabase db push, no se publican
supabase/migrations/

# scripts de despliegue/operación local, no forman parte de la app
scripts/

# builds descargados localmente (van adjuntos al Release, no al repo)
*.apk
*.aab
